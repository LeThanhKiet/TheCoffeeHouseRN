import React from "react";
import { View, Text, StyleSheet } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const BookingScreen = () => {
    <SafeAreaView>
        <Text>Đặt hàng Here</Text>
    </SafeAreaView>;
};

export default BookingScreen;
